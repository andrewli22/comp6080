import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    secondary: {
      light: '#ffbb52',
      main: '#fe8a1e',
      dark: '#c55b00',
      contrastText: '#fff',
    },
    gold: {
      main: '#e8b923',
    },
    coral: {
      main: '#ff5a5e',
      contrastText: '#fff',
    },
  },
});
